# Scratch delivery output only (recorded walkthroughs, one-off staged
# artifacts) -- never application source, so never tracked.
demo/

# Claude Code session/agent tooling state (e.g. isolated worktree
# checkouts spawned for background subagents) -- never application
# source, so never tracked.
.claude/

# docker-compose deploy secrets (see .env.example) -- never commit real values.
.env
frontend/test-results/
dist/
backups/

# Server-specific Compose additions (e.g. publishing Postgres to Central
# Command on another server). Never committed.
docker-compose.override.yml
